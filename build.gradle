plugins {
    id 'java'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'ca.guibi.tetris.App'
        )
    }
}

group = 'ca.guibi.tetris'
description = 'Tetris'
java.sourceCompatibility = JavaVersion.VERSION_16

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}
